


Spring:
  application:
    name: popcat
  jpa:
    hibernate:
      ddl-auto: update  # (1) ??? ?? ??
    show-sql: false      # (2) SQL ?? ??
    properties:
      hibernate:
        format_sql: true  # (3) SQL pretty print
        dialect: org.hibernate.dialect.MariaDB103Dialect
  datasource:
    url: jdbc:mariadb://svc.sel4.cloudtype.app:30792
    username: root
    password: 152390
    driver-class-name: org.mariadb.jdbc.Driver
